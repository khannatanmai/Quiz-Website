
	<div class="header">
  		<div class="container">
        <h1>Quiz App</h1>
  		</div>
  </div>
	
  <div class="sub_genre">
  <div class="container">
    <div class="row">
      <div class="col-xs-4">

        <%= image_tag @sub_genre.image %>
        <h2><%= @sub_genre.name %></h2>
        
        <div class="cards row">
          <% @quizzes.each do |q| %>
          <div>
            <h2><%= link_to q.name,quiz_path(q) %></h2>
          </div>
          <% end %>
        </div>

      </div>
    </div>
  </div>
  <%= link_to "Back to Main", "/genres" %>
  <%= link_to "Subgenre Leaderboard", "/scores/sub_genre/"+@sub_genre.id.to_s %>
</div> 